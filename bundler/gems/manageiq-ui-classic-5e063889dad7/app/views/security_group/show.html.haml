#main_div
  - if %w(instances network_ports security_policy_rules_as_source security_policy_rules_as_destination custom_button_events).include?(@display) && @showtype != "compare"
    = render :partial => "layouts/gtl", :locals => {:action_url => "show/#{@record.id}"}
  - elsif @showtype == 'main'
    = render :partial => "layouts/textual_groups_generic"
